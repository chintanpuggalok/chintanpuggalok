---
// The static full profile is the default experience. The interactive portfolio
// remains opt-in at /ask so no AI flow loads until a visitor chooses Ask AI.
import ProfilePage from "./profile.astro";
---

<ProfilePage />
